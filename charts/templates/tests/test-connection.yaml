apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cosci.fullname" . }}-test-connection"
  labels:
    {{- include "cosci.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cosci.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
